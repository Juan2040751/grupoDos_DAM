<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/backgroundColor"
        tools:context=".view.ChallengesActivity"
        android:orientation="vertical">


        <include
            android:id="@+id/challengesToolbar"
            layout="@layout/challenges_toolbar" />

        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/navigationContainer"
            android:name="com.example.grupodos_dam.view.fragment.HomeChallengesFragment"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:defaultNavHost="true"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.878"
            app:layout_constraintStart_toEndOf="@id/challengesToolbar"
            app:layout_constraintTop_toBottomOf="@+id/challengesToolbar"
            app:layout_constraintVertical_bias="0.0"

            />

    </LinearLayout>

</layout>